template:
  url: https://example.com/templates/invoice-v2.pdf
  type: fillable

mapping:
  pdf:
    field:
      invoiceNumber: order.id
      customerName: customer.name
      totalAmount: total

metadata:
  source: template
